<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="OxygenNEL.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OxygenNEL"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:component="using:OxygenNEL.Component"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Image x:Name="BackgroundImage" Stretch="UniformToFill" Visibility="Collapsed" Grid.RowSpan="2"/>
        <MediaPlayerElement x:Name="BackgroundVideo" 
                            Stretch="UniformToFill" 
                            Visibility="Collapsed" 
                            Grid.RowSpan="2" 
                            AutoPlay="False" 
                            AreTransportControlsEnabled="False"/>

        <Grid Grid.Row="0" x:Name="AppTitleBar" VerticalAlignment="Top" Background="Transparent" Margin="0,-1,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,6,0,6">
                <TextBlock x:Name="AppTitleIcon" Text="O₂" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                <TextBlock x:Name="AppTitleTextBlock" Text="Oxygen NEL" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <NavigationView Grid.Row="1" x:Name="NavView"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsPaneToggleButtonVisible="False"
                        IsSettingsVisible="True"
                        SelectionChanged="NavView_SelectionChanged"
                        Loaded="NavView_Loaded"
                        SizeChanged="NavView_SizeChanged">
            <NavigationView.PaneHeader>
                <component:UserProfileCard x:Name="UserProfile"/>
            </NavigationView.PaneHeader>
            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <Grid Grid.Row="1" IsHitTestVisible="False">
            <component:NotificationHost HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,16,0,0"/>
        </Grid>

        <Grid Grid.Row="1" x:Name="AuthOverlay" Visibility="Collapsed">
            <Frame x:Name="OverlayFrame"/>
        </Grid>

        <Grid x:Name="MusicPlayerPanel" 
              Grid.RowSpan="2"
              HorizontalAlignment="Right" 
              VerticalAlignment="Bottom"
              Margin="0,0,20,20"
              Visibility="Collapsed">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="MusicPlayerTransform"/>
            </Grid.RenderTransform>
            <Border Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="12,8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Opacity="0.85">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon x:Name="MusicIcon" Glyph="&#xE8D6;" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock x:Name="MusicTitle" Text="未播放" VerticalAlignment="Center" MaxWidth="100" TextTrimming="CharacterEllipsis"/>
                    <Button x:Name="MusicPlayPauseBtn" Click="MusicPlayPauseBtn_Click" Padding="6,4" Background="Transparent" BorderThickness="0">
                        <FontIcon x:Name="MusicPlayPauseIcon" Glyph="&#xE768;" FontSize="14"/>
                    </Button>
                    <Slider x:Name="MusicProgressSlider" Width="100" Minimum="0" Maximum="100" Value="0" 
                            ValueChanged="MusicProgressSlider_ValueChanged"
                            VerticalAlignment="Center"/>
                    <TextBlock x:Name="MusicTimeText" Text="0:00" VerticalAlignment="Center" FontSize="12" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" MinWidth="32"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
